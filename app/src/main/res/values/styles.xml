<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="My_CheckBox" parent="Theme.AppCompat.Light">
        <item name="colorControlNormal">@color/title1</item>
        <item name="colorControlActivated">@color/title2</item>
    </style>

    <style name="AppTheme.FullScreen" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>

    <style name="anim_popup_bottom_bar">
        <item name="android:windowEnterAnimation">@anim/popup_bottom_bar_in</item>
        <item name="android:windowExitAnimation">@anim/popup_bottom_bar_out</item>
    </style>

    <style name="anim_popup_left_bar">
        <item name="android:windowEnterAnimation">@anim/popup_left_bar_in</item>
        <item name="android:windowExitAnimation">@anim/popup_left_bar_out</item>
    </style>


</resources>